// Optional: copy to `js/app-config.js` and set your API base.
// Make sure it loads BEFORE `js/auth.js` in your HTML.
window.APP_CONFIG = {
  // Examples:
  // API_BASE: "https://your-domain.vercel.app/api"   // if you use Vercel rewrites
  // API_BASE: "https://your-backend.example.com/api" // direct backend URL
  API_BASE: ""
};

