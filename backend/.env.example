PORT=4000
NODE_ENV=development
JWT_SECRET=change_me_super_secret
JWT_EXPIRES_IN=1d

# Choose database engine: sqlite | postgres
DB_KIND=postgres

# Postgres (shared DB across devices). Example:
# DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=require
DATABASE_URL=postgresql://postgres:ZhHRyWRciSpprbG4@db.ywiroprfuldqxhezzrlm.supabase.co:5432/postgres

# SQLite DB path (relative to backend/) - used when DB_KIND=sqlite
DB_PATH=./data/app.db

# CORS (frontend origin)
CORS_ORIGIN=*
EMAILJS_SERVICE_ID=service_c88d2do
EMAILJS_TEMPLATE_ID_RESET=template_djd5hbw
EMAILJS_PUBLIC_KEY=oj1LyYs-QHqc7enhg

